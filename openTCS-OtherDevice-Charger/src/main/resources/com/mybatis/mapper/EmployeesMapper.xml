<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mybatis.mapper.EmployeesMapper">
    <select id="getEmployee" parameterType="Integer" resultType="Employees">
      select * from employees where id=#{id}       
    </select>
   <select id="getAllEmployee" resultType="Employees">
        select * from employees
    </select>
     <insert id="addEmployee" parameterType="Employees">
        insert into employees (id,first_name,last_name,email_address)
        values(#{id},#{first_name},#{last_name},#{email_address})
    </insert>
     <update id="updateEmployee" parameterType="Employees">
        update employees set id=#{id},first_name = #{first_name},last_name=#{last_name},email_address=#{email_address} where id = #{id}
    </update>
      <delete id="deleteEmployee" parameterType="Integer">
        delete from employees where id= #{id}
    </delete>
</mapper>